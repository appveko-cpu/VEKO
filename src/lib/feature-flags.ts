/**
 * FEATURE FLAGS — VEKO
 *
 * ACCESS_SYSTEM_ENABLED
 *   false → système essais/plans/paiement en veille (mode démo seul actif)
 *   true  → système complet actif (essais, PricingModal, UpgradeModal, TrialBanner…)
 *
 * Pour réactiver : passer à true et redéployer.
 */
export const ACCESS_SYSTEM_ENABLED = false;
